{
  "name": "shaman-api-solution",
  "version": "2.0.4",
  "description": "Solution wrapper for shaman-api projects.",
  "main": "index.js",
  "private": true,
  "scripts": {
    "restore": "sf install",
    "build": "sf build",
    "publish": "sf publish",
    "debug": "sf build && sf serve --project=shaman-api-sample",
    "sample": "sf serve --project=shaman-api-sample"
  },
  "license": "MIT",
  "dependencies": {
    "shaman-factory": "^2.0.0"
  }
}